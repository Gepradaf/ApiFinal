pipeline {
    agent any
    
    stages {
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Aquí podemos añadir comandos para despliegue real si es necesario
            }
        }
    }
}
